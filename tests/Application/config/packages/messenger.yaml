framework:
    messenger:
        default_bus: sylius.command_bus
        buses:
            sylius.command_bus:
                middleware:
                    - 'validation'
                    - 'doctrine_transaction'
            sylius.event_bus:
                default_middleware: allow_no_handlers